import React from "react";

const Subheaqding=(props)=>{
    return(
        <>
        <h2 className="text-center relative top-[-70px] text-[20px] text-gray-600">{props.text}</h2>
        </>
    );
}
export default Subheaqding;